# 项目范围与成本红线

## 当前范围

初始化作品集仓库、六个模块目录、环境清单、证据和任务看板。只进行本地只读环境探测；后续八周工作仅作为 backlog。当前云资源支出授权为 **US$0**。

## 后续规划范围

先完成本地 GPU、Linux、容器和指标基线，再逐步完成 Kubernetes/EKS 可观测性、Slurm/NCCL、单 GPU LLM 推理三条主线。每次实验记录命令、配置、日志、截图、结果、成本、清理状态；未执行或不支持项明确标注，不以模拟结果替代实测。

不包含生产 SLA、大模型训练、常驻 GPU 集群、多节点 H200/p5en 默认示例或未经批准的付费服务。多 GPU 测试仅作为另行确认预算的可选目标。

## 成本红线

- 文档建议的八周规划硬上限为 **US$150**，不是本次支出授权，也不是已配置的云端硬停机制。
- 启动云实验前必须明确区域、实例、数量、运行时长和单次预算，核对当时价格。累计实际费用 + 未结算估算 + 本次最坏费用不得超过批准额度；未知费用时不启动。
- 规划告警：US$37.50 / 75 / 120 / 150（25% / 50% / 80% / 100%）。**W2 D8 已配置**（见 [aws/cost-safety.md](aws/cost-safety.md)），但告警是事后通知，不是硬停机制，超支前后都要靠人工核对清理。
- 默认 GPU 最大 1 台；空闲计算节点为 0。实验结束即清理，不跨夜常驻；必须事先写明结束时间和销毁负责人。
- EKS 阶段结束销毁控制面，不能把 worker 缩到 0 当作全部清理完成。
- 优先本地实验和 port-forward；创建 NAT、负载均衡器、共享文件系统、大容量存储前将其纳入预算。
- 清理检查包括 EC2、EKS、ELB、NAT、EBS、快照、EIP、S3/日志保留及 CloudFormation；停止实例不等于费用归零。
- 每次实验更新 [成本台账](evidence/cost-log.csv)，后续账单到齐后再次核对。

以上是本项目的操作约束，多数条目仍是"人工纪律"，不是云平台强制执行的硬限制。**例外**：W2 D8 已经在账户里
真实创建了 Budget（$150/月）和 25/50/80/100% 四条告警（见 [aws/cost-safety.md](aws/cost-safety.md)），
这是目前唯一落地的自动化护栏；GPU 实例、EKS 等计费资源本身仍未创建，本项目至今未产生任何实际云端支出。

## 验收与数据边界

每个实验完成时必须可复现、证据完整、成本核对、资源清理。仓库不提交凭据、token、账户 ID、私有端点、个人进程列表或模型权重。公开发布前审阅证据；目标仓库由用户提供，当前为 public；只上传已审阅的非敏感项目文件和版本概要。

